# The name of the application
spring.application.name=grammar-visualizer

# JPA settings
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

# Base path for all API endpoints
spring.mvc.servlet.path=/api
